fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-docx>=1.1.2
groq>=0.9.0
anthropic>=0.28.0
python-dotenv>=1.0.1
httpx>=0.27.0
boto3>=1.34.0

# Dev-only: an in-process S3-API-compatible mock server, so the direct-to-S3
# chunked upload flow (Server/scripts/s3_storage.py) can be fully exercised
# without real AWS credentials. Run it with `moto_server -p 5001`. Not needed
# once S3_ENDPOINT_URL in .env points at real AWS.
moto[s3,server]>=5.0.0

# Dev-only: used by scripts/validate_large_upload.py to drive/measure a large
# end-to-end chunked upload from outside the browser.
requests>=2.31.0
psutil>=5.9.0
